## Agent skills

### Issue tracker

Issues tracked in GitHub Issues (`matthewrosse/jira-server-mcp`), via `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout — `CONTEXT.md` + `docs/adr/` at repo root. See `docs/agents/domain.md`.
