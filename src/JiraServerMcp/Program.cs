// The MCP host, CLI verbs, and composition root land in later phases. Diagnostics go to
// standard error: standard output belongs to the protocol stream (ADR-0002).
await Console.Error.WriteLineAsync("jira-server-mcp: not implemented yet.");
return 1;
